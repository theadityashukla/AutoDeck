# Data
*.pdf
extracted_images/
chroma_db/

# Models
gemma-3-12b-mlx/
gemma3-12b-int4/

# Secrets
# NOTE: this is v1's API-token directory, not v2's design tokens — those live in
# config/tokens/ precisely so this rule cannot swallow them (see PHASE-0 handover).
tokens/
.env

# Python
venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# System
.DS_Store
# Temp render files
rendered_slides/
renderer_test_output/
test_*.pptx
test_*.png

# --- v2 ---
# Per-build artifacts (IR versions, verdicts, renders) — plan §4
runs/
# Eval outputs; the harness and fixtures under evals/ ARE tracked
evals/out/
# Environments
.venv/
# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
# Licensed font binaries — see the Phase 0 font spike before committing any
fonts/*.ttf
fonts/*.otf

