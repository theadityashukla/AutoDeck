"""OOXML theme and slide-master generation from DesignTokens (D1, §6.8).

Two onboarding modes (task 3a.1): `master_builder.py` generates a theme from `tokens.json`
(mode b, built fully); `extract.py` reads one back out of an existing client template (mode
a, built against a synthetic template only — see its module docstring and B31/Q5).

Owning phase: Phase 0 (spike 0.4) / Phase 3a. See docs/AUTODECK_V2_PLAN.md §5 for the full tree.
"""
